@model MetaOMS.ViewModels.ShippingVM
@{
    ViewData["Title"] = "AddorUpdateShippingMethod";
}

@{ Layout = null; }

<div class="row">
    <div class="col-md-12">
        <form asp-action="AddorUpdateShippingMethod" asp-route-id="@Model.ShippingVMId" onsubmit="return jQueryAjaxPost(this);">

            <input type="hidden" asp-for="ShippingVMId" />
            <div class="form-group">
                <input id="CurrentPage" type="hidden" asp-for="CurrentPage" />
                <label asp-for="ShippingVMMethod" class="control-label"></label>
                <div class="input-group">

                    <input asp-for="ShippingVMMethod" class="form-control" />
                </div>
                <span asp-validation-for="ShippingVMMethod" class="text-danger"></span>
            </div>


            <div class="form-group">
                <div class="col-md-6 offset-md-3">
                    <input type="submit" value="Submit" class="btn btn-success text-white btn-block" />
                </div>
            </div>
        </form>
    </div>
</div>